const products = require("./products");
const totalDiscounts = require("./totalDiscounts");
const productDiscounts = require("./productDiscounts");

module.exports = {
  products,
  totalDiscounts,
  productDiscounts
};
